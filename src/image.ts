// import {compress, compressAccurately, EImageType} from 'image-conversion'

// export const imageCompress = async (file: Blob, quality: number, type: 'PNG' | 'JPEG' = 'JPEG') => await compress(file, {quality, type: EImageType[type]})

// export const imageCompressAccurately = async (file: Blob, size: number, type: 'PNG' | 'JPEG' = 'JPEG') => await compressAccurately(file, {size, type: EImageType[type]})

export const a = 1
